# Optional. The game remains playable without Redis.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Required for production leaderboard writes.
LEADERBOARD_SIGNING_SECRET=

# Optional canonical URL for metadata and challenge links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
